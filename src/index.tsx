import React from 'react';
import ReactDOM from 'react-dom';
import './styles.scss';

import SelectPro from '../dist';

const root = document.createElement('div');
document.querySelector('body').appendChild(root);

ReactDOM.render(<div />, root);
